<!-- generated by rulegate; edit .rulegate/ instead -->

## Commands

- `pnpm install` · `pnpm build` · `pnpm test` (Vitest) · `pnpm lint` · `pnpm verify`
- `pnpm -r exec tsc --noEmit` — typecheck across the workspace
- Single test file: `pnpm vitest run <path>` · single case: add `-t "<name>"`
- `RULEGATE_TEST_DIST=1 pnpm test` — exercises the **built** binary. The normal suite
  aliases `@rulegate/*` to source, so nothing else catches a broken `exports` map or a
  regressed exit code. Run it after `pnpm build`; it has already caught one real
  regression.

Golden fixtures live in `fixtures/<tool>/{input,expected}` and are asserted byte-exact;
detect fixtures use `fixtures/<tool>-detect/{positive,negative}`, and import fixtures use
`fixtures/<tool>-import/{input,expected}` — a repo with the tool's native files and no
`.rulegate/`, plus the canonical rules `read()` must recover from it. Regenerate with
`pnpm build && pnpm fixtures:update` — it prints the change set and writes nothing without
`--yes`, and it refuses to run when `CI` is set. See `fixtures/README.md`.

`rulegate init` is the on-ramp for a repository that has never used Rulegate: it
detects which tools are configured, imports their configs into canonical rules, collapses
the copies that arrive from several tools at once, and prints every file it would create,
modify or leave alone. It **writes nothing without `--yes`**. Applying takes ownership of
the files it imported from, copying each original to `.rulegate/backup/` first. On a
repository that already has a `.rulegate/` it says so and does nothing.

`rulegate sync` also **removes the artifacts no enabled adapter produces any more** —
delete a rule and its `.cursor/rules/*.mdc` goes with it, instead of being left on disk at
exit 0 for Cursor to keep loading. Every removal is copied to `.rulegate/backup/` first,
and an orphan edited since Rulegate wrote it is refused rather than deleted. Set
`options.backup: false` in the manifest to delete without the copy.

`rulegate check` is `sync`'s read-only twin: it renders the same plan in memory, compares
it to disk, and exits 1 with a unified diff when they differ. **Clean means `sync` would
write nothing and delete nothing** — a stale artifact, a hand-edited one, a missing one, a
file Rulegate never generated standing where its output goes, and an orphan still on disk
all count, each with the recovery hint `sync` would give. The diff runs from disk to
render, so `+` lines are what `sync` will write; changed lines show trailing spaces and
tabs as `·` and `␉`, and a missing final newline is marked, because those are the
differences a hash sees and a diff otherwise hides. It holds a filesystem with no write
methods on it and never calls `applyPlan`. `--staged` checks the **git index** instead of
the working tree, which is what a pre-commit hook needs: both sides come from the index, so
an unstaged edit never blocks a commit and stale staged artifacts never slip through one.
Outside a git working tree it refuses rather than quietly checking the working tree.
`packages/core/src/git/` is the only directory in shipped source allowed to spawn a process,
it runs three read-only git subcommands, and `invariants.test.ts` pins both facts.

`rulegate sync --import` merges a hand-edit on a generated file back into
`.rulegate/`. It prints the merge as a diff and **writes nothing without `--yes`**. The
ancestor is the current render, valid only while `state.json`'s recorded hash still matches
it — if the canonical source moved on too, the version you edited cannot be reconstructed
from anything, and the file is **refused** rather than merged from a guessed ancestor.
Matching is by position via `Artifact.provenance.ruleIds`, because a rule's id does not
survive rendering; a file whose section count no longer matches the rules that produced it
is refused for the same reason. `sync --force` is the other half: it discards the edit
instead of recovering it, after copying the original to `.rulegate/backup/`.

`rulegate restore [path...]` puts back the originals under `.rulegate/backup/` — the undo
for both `sync --force` and orphan deletion. Like `init` it **writes nothing without
`--yes`**, and it copies raw bytes, so a CRLF or BOM original comes back exactly as it was.
With no arguments it restores everything in the backup.

`rulegate doctor` reports which tools are configured, which files each will actually
load, in what order, whether they are in sync, and roughly what they cost in tokens. It is
read-only and **exits 0 even when it has warnings** — `check` owns exit 1 for drift, and a
command that reports a correct permanent condition as a CI failure is one people mute.
`--no-global` skips the user-level probe; `--json` emits the whole report.

`rulegate adapter new <tool>` scaffolds an adapter — a working concatenated-Markdown
stub, its three fixture layouts, its tests, and its registration in the registry, the
CLI's dependencies, the Vitest alias and RFC-0001 §4.1. It is the one command aimed at
contributors rather than users: it writes into a checkout of this monorepo and refuses
anywhere else. Like `init` it **writes nothing without `--yes`**, and it never overwrites —
every generated path must be absent and every patched file must exist, checked before the
first write so a collision leaves the tree untouched. Registration is part of the scaffold
because `registry.test.ts` pins `ADAPTERS` to the directory listing: an unregistered
adapter fails the suite. What is left for the author is the artifact path, the real
precedence rules in `src/docs.ts`, and a hand-written golden.

Exit codes: `0` ok · `1` drift or failure · `2` usage. CI reads the code, not the
message, so a usage error must never be reported as drift.
